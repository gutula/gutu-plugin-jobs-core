# Jobs Core Glossary

| Term | Meaning |
| --- | --- |
| Jobs Core | Background jobs, schedules, and execution metadata. |
| jobs.executions | Capability published by this plugin manifest. |
| jobs.executions.schedule |  |
| crm.sync-segments | Job definition queued on `crm-sync`. |
| files.scan-uploads | Job definition queued on `files-security`. |
| notifications.dispatch | Job definition queued on `notifications`. |
| notifications.dispatch.immediate | Job definition queued on `notifications`. |
| notifications.dispatch.scheduled | Job definition queued on `notifications`. |
| notifications.dispatch.digest | Job definition queued on `notifications-digest`. |
| notifications.dispatch.retry | Job definition queued on `notifications-retry`. |
| ai.runs.intake | Job definition queued on `ai-intake`. |
| ai.runs.verify | Job definition queued on `ai-verification`. |
| workflow.approvals.remind | Job definition queued on `workflow-approvals`. |
| workflow.approvals.escalate | Job definition queued on `workflow-escalations`. |
| company.work-intakes.classify | Job definition queued on `company-intake`. |
| company.work-intakes.recover | Job definition queued on `company-recovery`. |
| Job definitions | Primary focus area for Jobs Core. |
| Retry policy | Primary focus area for Jobs Core. |
| Execution metadata | Primary focus area for Jobs Core. |
